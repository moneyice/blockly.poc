let monthSalary = annualMonthlyTaxableSalary() - annualAllowance() - annualSeriousIllnessTreatment() - annualMortgageInterest() - annualSpecialDeductionAdjustment();
if (monthSalary > 0) {
let personTax = calculateTax(monthSalary);
return personTax;
}
return 0;

