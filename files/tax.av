let monthSalary = annualMonthlyTaxableSalary() - annualAllowance() - annualSeriousIllnessTreatment() - annualMortgageInterest();
if (monthSalary > 0) {
  let personTax = calculateTax(monthSalary);
  return personTax;}
return 0;
